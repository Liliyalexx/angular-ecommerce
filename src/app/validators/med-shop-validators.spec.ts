import { MedShopValidators } from './med-shop-validators';

describe('MedShopValidators', () => {
  it('should create an instance', () => {
    expect(new MedShopValidators()).toBeTruthy();
  });
});
